name = "image-upload-worker"
main = "image-upload-worker.js"
compatibility_date = "2024-12-01"

# Environment variables (set these via Cloudflare dashboard or wrangler secrets)
# Do NOT commit your actual API token to version control
[vars]
# CLOUDFLARE_ACCOUNT_ID will be set via wrangler secret

# For secrets (use: wrangler secret put CLOUDFLARE_API_TOKEN)
# CLOUDFLARE_API_TOKEN should be set as a secret, not a regular var
